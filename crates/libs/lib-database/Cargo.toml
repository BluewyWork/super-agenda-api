[package]
edition = "2021"
name = "lib-database"
version = "0.1.0"

[dependencies]
futures = "0.3.30"
mongodb = { version = "2.8.2", features = ["tokio-runtime"] }
serde = { version = "1.0.198", features = ["derive"] }
